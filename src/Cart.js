
import CartItem from './CartItem'


export default class Cart {                    // проверка если есть
    add(item) {                 // добавить в корзину объект, создание объекта
        new CartItem (item)
        }
    }

